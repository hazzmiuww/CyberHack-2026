/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow cross-origin access from network IP
  allowedDevOrigins: ['192.168.157.1'],
  reactStrictMode: true,
};

module.exports = nextConfig;
